<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Enter Measurements</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url('/images/fabric2.jpeg') no-repeat center center fixed;
      background-size: cover;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      backdrop-filter: blur(8px); /* 👈 key for glass effect */
      -webkit-backdrop-filter: blur(8px); /* Safari support */
      border: 1px solid rgba(255, 255, 255, 0.18);
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      padding: 1rem;
    }
    .card {
      background: rgba(14, 11, 11, 0.95);
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 0 25px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 600px;
      backdrop-filter: blur(5px);
    }
    .form-label {
      font-weight: 500;
      color: white;
    }
    .btn-next {
      border-radius: 30px;
      font-weight: 500;
    }
    h3 {
      font-weight: 600;
      color: #f2ebeb;
    }
  </style>
</head>
<body>

  <div class="card">
    <h3 class="text-center mb-4">Enter Measurements</h3>
    <form method="POST" action="/measurements">
      <div class="row g-3">
        <div class="col-md-6">
          <label class="form-label">Chest</label>
          <input type="text" name="chest" class="form-control" required />
        </div>
        <div class="col-md-6">
          <label class="form-label">Waist</label>
          <input type="text" name="waist" class="form-control" required />
        </div>
        <div class="col-md-6">
          <label class="form-label">Hip</label>
          <input type="text" name="hip" class="form-control" />
        </div>
        <div class="col-md-6">
          <label class="form-label">Sleeve</label>
          <input type="text" name="sleeve" class="form-control" />
        </div>
        <div class="col-md-6">
          <label class="form-label">Shoulder</label>
          <input type="text" name="shoulder" class="form-control" />
        </div>
        <div class="col-md-6">
          <label class="form-label">Length</label>
          <input type="text" name="length" class="form-control" />
        </div>
      </div>
      <div class="d-grid mt-4">
        <button class="btn btn-primary btn-next" type="submit">Next</button>
      </div>
    </form>
  </div>

</body>
</html>
