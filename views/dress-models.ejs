<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Select <%= dressType %> Design</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url("/images/fabric.jpg") no-repeat center center fixed;
      background-size: cover;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      backdrop-filter: blur(8px); /* 👈 key for glass effect */
      -webkit-backdrop-filter: blur(8px); /* Safari support */
      border: 1px solid rgba(255, 255, 255, 0.18);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .wrapper {
      background: rgba(255, 255, 255, 0.9);
      border-radius: 1rem;
      padding: 2rem;
      max-width: 700px;
      width: 100%;
      text-align: center;
      margin-bottom: 2rem;
    }

    .model-card {
      cursor: pointer;
      border: 2px solid transparent;
      border-radius: 1rem;
      background-color: #f8f9fa;
      transition: all 0.3s ease;
      padding: 0.75rem;
    }

    .model-card:hover {
      border-color: #0d6efd;
      background-color: #e7f1ff;
      transform: scale(1.02);
    }

    .model-img {
      width: 100%;
      height: 150px;
      object-fit: cover;
      border-radius: 0.75rem;
      margin-bottom: 0.5rem;
    }

    .model-label {
      font-size: 1.1rem;
      font-weight: 600;
      color: #333;
      text-align: center;
    }
  </style>
</head>
<body>

  <div class="wrapper">
    <h4 class="mb-4">Select a <%= dressType %> Design</h4>
    <form method="POST" action="/dress-models">
      <div class="row g-4">
        <% models.forEach(model => { %>
          <div class="col-md-6">
            <button type="submit" name="dressModel" value="<%= model %>" class="btn w-100 model-card text-center">
              <img src="/images/<%= dressType %>-<%= model.toLowerCase().replace(/ /g, '-') %>.jpeg"
                   alt="<%= model %>"
                   onerror="this.onerror=null;this.src='/images/fallback.jpg';"
                   class="model-img" />
              <div class="model-label"><%= model %></div>
            </button>
          </div>
        <% }) %>
      </div>
    </form>
    <div class="text-center mt-3">
      <a href="/dress" class="btn btn-outline-secondary">← Back to Dress Types</a>
    </div>
  </div>

</body>
</html>