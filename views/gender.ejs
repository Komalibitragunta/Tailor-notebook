<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Select Gender | Tailor Notebook</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url("/images/fabric.png") no-repeat center center fixed;
      background-size: cover;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      backdrop-filter: blur(8px); /* 👈 key for glass effect */
      -webkit-backdrop-filter: blur(8px); /* Safari support */
      border: 1px solid rgba(255, 255, 255, 0.18);
      margin: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .card-wrapper {
      background: rgba(255, 255, 255, 0.9);
      border-radius: 1rem;
      padding: 2rem;
      max-width: 700px;
      width: 100%;
      box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    .gender-card {
      cursor: pointer;
      border: 2px solid transparent;
      border-radius: 1rem;
      transition: all 0.3s ease;
    }

    .gender-card:hover {
      border-color: #0d6efd;
      transform: scale(1.05);
    }

    .gender-img {
      width: 50%;
      border-radius: 1rem;
      object-fit: cover;
      max-height: 100px;
      width: 20%;
    }

    .gender-label {
      font-size: 1.2rem;
      font-weight: 600;
      margin-top: 0.5rem;
    }
  </style>
</head>
<body>

  <div class="card-wrapper">
    <h3 class="mb-4">Select Your Gender</h3>
    <form method="POST" action="/gender">
      <div class="row g-4">
        <div class="col-md-6">
          <button type="submit" name="gender" value="male" class="btn p-0 w-100 gender-card bg-transparent">
            <img src="/images/male-avatar.jpg" alt="Male" class="gender-img" />
            <div class="gender-label text-dark">Male</div>
          </button>
        </div>
        <div class="col-md-6">
          <button type="submit" name="gender" value="female" class="btn p-0 w-100 gender-card bg-transparent">
            <img src="/images/female-avatar.jpeg" alt="Female" class="gender-img" />
            <div class="gender-label text-dark">Female</div>
          </button>
        </div>
      </div>
    </form>
  </div>

</body>
</html>
