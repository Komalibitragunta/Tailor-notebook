<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard | Tailor Notebook</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url('/images/fabric.jpeg') no-repeat center center fixed;
      background-size: cover;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    .dashboard-card {
      background: rgba(255, 255, 255, 0.95);
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 0 25px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 800px;
      backdrop-filter: blur(5px);
    }
    .card-stat {
      padding: 1.5rem;
      border-radius: 1rem;
      color: #fff;
    }
    .card-customers { background: #0d6efd; }
    .card-orders { background: #198754; }
    .card-appointments { background: #17a2b8; }
    .btn-group a {
      margin: 0.5rem;
    }
  </style>
</head>
<body>

  <div class="dashboard-card">
    <h3 class="text-center mb-4">Tailor Notebook Dashboard</h3>
    <div class="row g-4 mb-4">
      <div class="col-md-4">
        <div class="card-stat card-customers text-center">
          <h5>Total Customers</h5>
          <h3><%= totalCustomers %></h3>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card-stat card-orders text-center">
          <h5>Total Orders</h5>
          <h3><%= totalOrders %></h3>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card-stat card-appointments text-center">
          <h5>Appointments</h5>
          <h3><%= totalAppointments %></h3>
        </div>
      </div>
    </div>

    <div class="btn-group d-flex justify-content-center flex-wrap">
      <a href="/customer" class="btn btn-outline-primary">Add New Customer</a>
      <a href="/customers" class="btn btn-outline-secondary">View Customers</a>
      <a href="/order" class="btn btn-outline-success">Recent Order</a>
      <a href="/logout" class="btn btn-outline-danger">Logout</a>
    </div>
  </div>

</body>
</html>
