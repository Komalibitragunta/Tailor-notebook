<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Confirmation</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url('/images/fabric.png') no-repeat center center fixed;
      background-size: cover;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      backdrop-filter: blur(8px); /* 👈 key for glass effect */
      -webkit-backdrop-filter: blur(8px); /* Safari support */
      border: 1px solid rgba(255, 255, 255, 0.18);
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      padding: 1rem;
    }
    .card {
      background: rgba(15, 12, 12, 0.95);
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 0 25px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 600px;
      backdrop-filter: blur(5px);
      color: white;
    }
    h3, h5 {
      font-weight: 600;
      color: white;
    }
    ul {
      padding-left: 0;
      list-style-type: none;
      color: white;
    }
    ul li {
      padding: 0.5rem 0;
      border-bottom: 1px solid #ddd;
    }
    .btn-group {
      margin-top: 1.5rem;
    }
  </style>
</head>
<body>

  <div class="card">
    <h3 class="text-center mb-4">Order Confirmation</h3>
    <p class="text-center">Thank you for your order! Your dress will be ready on the delivery date below.</p>

    <h5 class="mt-4">Summary:</h5>
    <ul>
      <li><strong>Name:</strong> <%= customer.name %></li>
      <li><strong>Phone:</strong> <%= customer.phone %></li>
      <li><strong>Gender:</strong> <%= customer.gender %></li>
      <li><strong>Dress Type:</strong> <%= customer.dressType %></li>
      <li><strong>Model:</strong> <%= customer.dressModel %></li>
      <li><strong>Total Amount:</strong> ₹<%= customer.payment.amount %></li>
      <li><strong>Method:</strong> <%= customer.payment.method %></li>
      <li><strong>Delivery Date:</strong> <%= customer.payment.deliveryDate %></li>
    </ul>

    <div class="btn-group d-flex justify-content-between">
      <a href="/dashboard" class="btn btn-outline-primary">Go to Dashboard</a>
      <a href="/customer" class="btn btn-primary">Add Another</a>
    </div>
  </div>

</body>
</html>
