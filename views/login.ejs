<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login | Tailor Notebook</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet"/>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url('/images/fabric.jpg') no-repeat center center fixed;
      background-size: cover;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    .login-card {
      background-color: rgba(52, 47, 47, 0.95);
      padding: 2rem 2.5rem;
      border-radius: 1rem;
      box-shadow: 0 0 30px rgba(201, 193, 193, 0.1);
      max-width: 400px;
      width: 100%;
      color: white;
    }

    .form-control {
      border-radius: 30px;
    }

    .btn-login {
      border-radius: 30px;
      font-weight: 500;
    }

    h2 {
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="login-card text-center">
    <h2>Login</h2>
    <p class="mb-4">Welcome back to Tailor Notebook</p>
    <form method="POST" action="/login">
      <div class="mb-3 text-start">
        <label class="form-label">Username</label>
        <input type="text" name="username" class="form-control" required>
      </div>
      <div class="mb-3 text-start">
        <label class="form-label">Password</label>
        <input type="password" name="password" class="form-control" required>
      </div>
      <button type="submit" class="btn btn-primary w-100 btn-login">Login</button>
    </form>
  </div>

</body>
</html>
